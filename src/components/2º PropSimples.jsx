function PropSimples (props) {
    return(
        <>
        <div className="propSimples">
            <h2>Olá, {props.nome} {props.idade}</h2>
        </div>
        </>
    )
}

export default PropSimples;
